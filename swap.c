#include <stdio.h>
#include <math.h>

int main()
{
    int n, firstDigit=9, lastDigit=8;
    firstDigit=firstDigit+lastDigit;
lastDigit=firstDigit-lastDigit;
firstDigit=firstDigit-lastDigit;
printf("after swaping%d\n",firstDigit);
printf("after swaping%d\n",lastDigit);

return 0;
}

